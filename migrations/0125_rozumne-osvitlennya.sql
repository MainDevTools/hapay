-- 0125: нова категорія РОЗУМНЕ ОСВІТЛЕННЯ (Розумний дім).
--
-- ⚠ ОДНЕ джерело: Rozetka c4638351 (чиста смарт-категорія, пошук-URL).
-- Epicentr umnoe-osveshchenie СВІДОМО НЕ взято: заміряно 2026-07-24 — лише
-- 34/60 позицій зі смарт-ознаками, решта — звичайні LED Osram/Eurolamp
-- (категорія стала б «лампи взагалі»). Allo окремої категорії не має (лише
-- smart-фільтр настільних — фільтри не реєструємо). Крос-матчер відсутній за
-- визначенням (1 джерело) → чистий per-store Omnibus (Hue/Yeelight преміум).
-- Бекфілу нема. Forward-only, ідемпотентно.

INSERT INTO category (name, slug) VALUES
  ('Розумне освітлення', 'rozumne-osvitlennya')
ON CONFLICT (slug) DO NOTHING;
