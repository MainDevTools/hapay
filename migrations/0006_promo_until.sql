-- 0006: дата кінця дії ціни/акції (schema.org priceValidUntil), де крамниця її дає.
-- Rozetka декларує реальну дату у JSON-LD (напр. знижки діють до 26.07); більшість
-- інших — не дають або ставлять генерично. NULL = невідомо. Показуємо лише коли дата
-- реальна (майбутня, у розумних межах) — фільтр у запиті, не тут.

ALTER TABLE store_product ADD COLUMN promo_until DATE;
