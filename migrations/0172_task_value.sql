-- 0172: пріоритезація черги за ЦІННІСТЮ сторінки (S17).
--
-- Заміряно 2026-07-26 на проді: 2244 задачі, з них **1073 жодного разу не збирали**,
-- 86 за добу, середній вік останнього збору 2.7 дня. Повний оберт черги ≈26 днів.
-- Для статутного 30-денного вікна (§5) це фатально: рівно тому `verified` = 0 при
-- 23 223 `declared` — історії просто немає з чого рахувати.
--
-- Ємність збору ми підняти не можемо (це 🧭-рішення оператора, звіт A–E). Але можемо
-- витрачати наявну на те, що дає продукт: сторінки, чиї товари стоять у крос-крамничних
-- групах, мають активні знижки або за ними стежать. Решта — хвіст, який усе одно не
-- встигає набрати придатної історії.
--
-- `slug` — категорія лістинга з конфігу (`source_listings` її й так віддає, сів досі
-- відкидав). `value_tier`: 0 висока / 1 звичайна / 2 хвіст.

ALTER TABLE collect_task ADD COLUMN IF NOT EXISTS slug TEXT;
ALTER TABLE collect_task ADD COLUMN IF NOT EXISTS value_tier SMALLINT NOT NULL DEFAULT 1;

-- Оренда сортує за (priority, value_key, not_before) — індекс під це.
CREATE INDEX IF NOT EXISTS ix_ct_value ON collect_task (priority, value_tier, not_before);
