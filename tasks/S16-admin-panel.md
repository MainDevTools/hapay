# Задача S16 — Повноцінна адмін-панель: метрики продукту + керування акаунтами

**Роль:** Виконавець · **Автор:** Диригент, 2026-07-26 · **Статус:** `E1 в роботі`
**Залежності:** S15 (ролі + гейти + admin_audit), S13 (verify/reset), S10/T16 (черга збору).

### 0. Навіщо
S15 дав ролі й мінімум дій, але панель неадекватна: метрик продукту нема взагалі,
акаунти без пошуку/пагінації, а `admin_audit` **write-only** (слід пишеться, прочитати
нічим). Оцінка оператора 2026-07-26: «немає адекватної адмін-панелі/метрики та немає
зручного методу керування акаунтами».

**Носій:** обидва (рішення оператора) — веб `hapay.today/admin` для широких таблиць і
графіків, додаток — для швидких дій.

### 1. Що показувати (числа з живої бази, 2026-07-26 — для калібрування)
- **Дані:** store_product 49 089 · price_snapshot 120 011 · discount_event 23 391 ·
  category 173 · source 30. Приріст за добу.
- **Детекція (серце продукту):** declared 23 003 · insufficient_history 340 ·
  pumped 29 · verified_provisional 19 · **verified 0**. Знайдено накачаних за 7 дн.
- **Збір:** по крамницях (остання успішна / остання спроба / збійних поспіль);
  черга 2245 задач, 1114 ok, 45 fail.
- **Акаунти:** усього/по ролях/verified/забанених; реєстрації 7/30 дн; активні
  (last_login) 7/30 дн.

### 2. Скоуп (П1–П4, усі ухвалені)
**П1 метрики** · **П2 акаунти** (пошук email, фільтр роль/стан, пагінація, картка юзера:
реєстрація, останній вхід, стеження, адмін-дії над ним) · **П3 журнал аудиту** (хто/що/
кому + фільтр) · **П4 дії** (підтвердити email вручну, надіслати скидання пароля,
видалити акаунт).

### 3. Схема (0171) — аудит мусить пережити видалення акаунта
`admin_audit` FK нині блокують `DELETE app_user` (actor_id NOT NULL, target_id RESTRICT).
Право на забуття вимагає видалення, слід аудиту вимагає збереження. Розв'язка:
денормалізувати email у сам запис + послабити FK.
```sql
admin_audit += actor_email TEXT, target_email TEXT   -- знімок на момент дії
actor_id/target_id → ON DELETE SET NULL; actor_id DROP NOT NULL
```
Так слід лишається читабельним («хто кому»), навіть коли акаунта вже нема.

### 4. Ендпойнти
| Метод | Шлях | Гейт |
|---|---|---|
| GET | `/api/admin/metrics` (розширений) | moderator+ |
| GET | `/api/admin/users?q=&role=&active=&page=` | moderator+ |
| GET | `/api/admin/users/{id}` (картка) | moderator+ |
| GET | `/api/admin/audit?actor=&action=&page=` | moderator+ |
| POST | `/api/admin/users/{id}/verify` | moderator+ |
| POST | `/api/admin/users/{id}/send-reset` | moderator+ |
| DELETE | `/api/admin/users/{id}` | **admin** |

### 5. БЕЗПЕКА
- Видалення — **лише admin**, незворотне: не себе, не останнього активного admin,
  moderator не має доступу взагалі. Аудит із денормалізованим email (див. §3).
- `send-reset` шле лист на ЧУЖУ адресу → rate-limit і аудит (інакше — вектор спаму).
- Ручний verify — аудит (обхід поштової перевірки мусить лишати слід).
- Пошук `q` — лише параметризований ILIKE (жодної конкатенації в SQL).
- Метрики не віддають ПЕРСОНАЛЬНИХ даних (лише агрегати).
- **Веб-панель:** токен у sessionStorage (не localStorage — гине із вкладкою);
  сторінка статична, усе через ті самі гейти; жодних секретів у HTML.

### 6. DoD
- [ ] E1: 0171-незалежні метрики + пошук/пагінація акаунтів + тести.
- [ ] E2: 0171 + картка + аудит + 3 дії + тести (захисти, аудит-слід).
- [ ] E3: веб `hapay.today/admin` — вхід, дашборд, таблиця, аудит, дії; живий smoke.
- [ ] E4: MAUI — дашборд, пошук, картка, аудит, дії; збірка 0 errors + прогін.

### 7. Guardrails
- Метрики — ФАКТИ з бази, без «оцінок здоров'я» пальцем у небо.
- `verified 0` не ховати: якщо показник нульовий, показуємо нуль (тихий нуль — дефект).
- Рахунки на великих таблицях мусять лишатись швидкими (заміряти, не гадати).

---
## Outcome
_(E1–E4)_
