# Captain-log — стан проекту й відновлення після скидування контексту

**Статус: живий документ.** Це **пам'ять Диригента між сесіями** і **якір відновлення**, коли Claude Code скидає/компактує контекст. Один принцип: **репо — істина, пам'ять — одноразова.** Якщо цей файл і реальність (git, тести, `tasks/*`) розходяться — вір репо, не переказу.

**START HERE для свіжої/скинутої сесії:** спершу [↻ Процедура відновлення](#-процедура-відновлення), потім [Поточний стан](#поточний-стан).

---

## ↻ Процедура відновлення

Скидування буває **двох типів** — не плутати:

| Тип | Що це | Загроза |
|---|---|---|
| **A. Свіжа сесія** | ти навмисно почав нову (Диригент/Виконавець) | нема, якщо є що прочитати (цей файл + bootstrap §05) |
| **B. Авто-компакт** | харнес сам сумаризував довгу сесію **посеред задачі** | 🔴 переказ міг **загубити** інваріант, точний під-крок, незакомічене |

**B — справжня небезпека:** сесія прокидається з переказом і продовжує, **довіряючи йому**. Головне правило: **переказ — підказка, не істина.**

### Що прочитати (у цьому порядку)
1. **`CLAUDE.md`** (корінь, коли з'явиться) АБО `workflow/02-invariants.md` — **тверді інваріанти** (їх найлегше загубити в переказі, тож завжди перечитати);
2. **цей файл → «Поточний стан»** — де ми, що в роботі, що заблоковано;
3. якщо посеред задачі — **`tasks/SNN-*.md` + його §Outcome** — що вже зроблено/лишилось;
4. **`git log --oneline -15`** і **`git status`/`git diff`** — що реально закомічено vs у робочому дереві;
5. `workflow/03-decisions.md` — щоб **не перерішувати** вирішене (+ відкрите O1–O9);
6. за потреби — `16-reyestr-ryzykiv.md` §16.5 (актуальні ризики).

### Перевірити (не вір пам'яті)
7. **Прогнати тести / `probe.py`** → поточний стан коду з **факту**, а не з переказу;
8. звірити «що я нібито робив» (переказ) з п.3–4 (Outcome + git). Розбіжність → вір Outcome+git.

### Аж тоді продовжити
9. Якщо задача не закінчена — резюмуй у 1–2 рядки «де я і наступний крок», і **тільки потім** пиши код/текст.
10. Якщо не сходиться (тести червоні там, де Outcome каже «зелено»; git порожній там, де переказ каже «зробив») — **STOP**, признач це оператору, не імпровізуй.

> Свіжа сесія (тип A) з нуля: бери промпт зі `workflow/05-bootstrap-prompts.md` (Диригент або Виконавець) — він уже веде за цим списком.

---

## Профілактика (щоб було що відновлювати)

Скидування безболісне, лише якщо **нічого критичного немає лише-в-пам'яті**. Дисципліна:

- **Комітити кожен зелений крок**, не в кінці задачі → компакт/збій не з'їдає незакомічене. (потребує `git init` — P6, ще не зроблено).
- **Оновлювати §Outcome брифа інкрементально** («зроблено досі: …»), не лише наприкінці.
- **Свіжа сесія Виконавця на задачу** (O2) → задача коротка, **до авто-компакту не доходить**. Це головна профілактика типу B: не давати сесії розростись.
- **Shutdown-ритуал:** наприкінці сесії дописати рядок у [Журнал сесій](#журнал-сесій) (де спинились + наступний крок). Тоді наступна сесія стартує з факту, а не з археології.
- **Стан — у файли, не в чат:** рішення → `03-decisions`; розходження з реальністю → відповідний §спеки + журнал `00-index`; де ми → сюди.

---

## Поточний стан

> Оновлювати при кожній зміні стану. Це те, що читає скинута/свіжа сесія **першим по суті**.

**Дата оновлення:** 2026-07-19

**Фаза:** 🚀 **ПРОДАКШН ЖИВИЙ на власному сервері.** Продукт = **«Хапай»** (T14), https://hapay.today відповідає `{"ok":true}` з HTTPS (Let's Encrypt до 16.10.2026). Перший збір на сервері: **379 позицій / 309 подій**, обидва джерела `[ok]`.

**Інфраструктура (2026-07-18, bare-metal — Docker відкинуто власником як «зайвий фрагмент»):**
- **Hetzner CX23** (Helsinki, IP `89.167.84.32`, Ubuntu 24.04), бекапи машини ввімкнені (+20%).
- **PostgreSQL 18.4** (PGDG) локально, слухає лише localhost. **Neon відкинуто** (власник: усе на своєму залізі).
- **API:** uvicorn у venv `/opt/hapay/venv`, systemd `hapay-api.service`, від користувача `hapay`, на 127.0.0.1:8080.
- **Caddy:** TLS + проксі, єдиний вихід назовні (ufw: лише 22/80/443). Env через `/etc/hapay/caddy.env` (лише домен — least privilege).
- **Збір:** `hapay-collect.timer` **на сервері** 2×/добу (05:23/17:23) — GH Actions `collect.yml` видалено (база приватна).
- **Бекап:** `hapay-backup.timer` щоночі 03:17 → **чекає `BACKUP_TARGET`** (офсайт; поки порожній — таймер навмисно падатиме). 🔴 налаштувати, коли назбирається тиждень історії.
- Секрети: `/etc/hapay/hapay.env` (chmod 600, поза git; пароль PG згенеровано скриптом).
- **Домен:** `hapay.today` (HostPro, A-запис → сервер; `.ua` вимагатиме ТМ — пізніше). ⚠ Нагадати оператору: **підтвердити email реєстрації** (вимога ICANN, інакше блок за ~15 днів).

**Репо: `github.com/MainDevTools/hapay` (ПУБЛІЧНИЙ, рішення власника; скан секретів чистий).** Повна історія (46+ комітів). Старий `cavemancinema/radar-znyzhok` — remote `old`, можна закинути. `gh` авторизовано під MainDevTools.

**Дані:** історія цін почалась заново 2026-07-18 (стару знищено 17.07 — T13; Neon-відкат не робили, бо Neon відкинуто). За ~30 днів → verified/pumped (S0.3).

**Ключові рішення останніх днів:** T12 (модель B: видимий шар без вердикту — реалізовано в UI), T13 (dbguard: руйнівні тести лише `TEST_DATABASE_URL`), **T14 (розворот «ЛОВИ знижки», назва «Хапай»; 🟡 суть відкрита — замінює модель B чи перевдягає; CC радить «перевдягнути»)**. gzip у колекторі (10.7× менше трафіку). **Розвідка електроніки:** Rozetka/Foxtrot/Moyo/Allo віддають картки звичайним GET; MPN-ключ (`SM-…`) наскрізний — headless НЕ потрібен (S7 застарів у частині передумови); ⚠ пастка суфіксів `AUXUA` при матчингу.

**Distributed-збір (S10) працює end-to-end (2026-07-18):** POST /api/ingest (токен+валідація) + collect --satellite. Точки збору обходять 403 ДЦ-фільтри: **сервер** (Pethouse/PetChoice), **дім оператора/друзів** (резидентний IP → Allo/Rozetka/EVA), **GH Actions/Azure** (Foxtrot/Moyo/Eldorado). Allo вже тече з дому оператора у вітрину (48 позицій, MPN у назвах). Додати колектора = +токен у INGEST_TOKENS.

**T14 закрито (2026-07-18):** агрегатор знижок («замінюємо», не «перевдягаємо»; дисент CC збережено). ⚠ **UI ще не перевдягнений** — каже «заявлено·не перевірено» / «знижки проти історії цін» (мова моделі B). Окремий крок.

**S11 авторизація (2026-07-19) — етапи 1-2 готові:**
- **Етап 1 (бекенд, live):** `0003_auth.sql` (app_user, ролі user/collector/moderator/admin, watchlist на user_id). `api/auth.py` — pbkdf2_sha256 (600k) + HS256 JWT, **чистий stdlib** (без зовн. крипто-ліб); JWT_SECRET з env (fail-loud <16 симв). Ендпойнти: register/login/me/watchlist. test_auth 11/11 + 9 живих у test_api. Зовні перевірено на hapay.today.
- **Етап 2 (клієнт MAUI, коміт `9d04b9e`):** екрани входу/реєстрації (один екран із перемикачем) + профіль; токен у **SecureStorage** (Keystore/Keychain, не plaintext); Bearer у ApiService; кнопка «Акаунт» у тулбарі. Адверсарне рев'ю (4 лінзи) — 1 підтв. баг виправлено: LoadAsync ловить збій розшифрування SecureStorage (оновлення ОС/зміна блокування) → анонім+RemoveAll, інакше **краш-цикл на старті**. **Скопійовано у VS-проєкт; збірку в VS оператором ще не проганяли.**
- ⚠ Тестовий акаунт `deploy-check@hapay.today` (user_id:1) створено при перевірці — оператору варто видалити (SQL надано).
- **Етап 3 (коміти `fc4fce9` бекенд + `0a72617` застосунок) — ЗРОБЛЕНО, чекає деплою+збірки:**
  - `require_collector` тепер приймає і статичний токен (S10), і JWT-акаунт із роллю `collector/moderator/admin` → провенанс `acct:<id>`.
  - **`/api/ingest/html`:** застосунок шле СИРИЙ HTML зі свого IP → **сервер парсить** адаптером (та сама валідація+персист, що й `/api/ingest`). Двофазно для hub: сервер робить `discover()`, повертає лендинги. Реєстр `HTML_SOURCES` (поки Allo — єдиний із серверним адаптером).
  - **`/api/collect/plan`:** сервер — авторитет над списком крамниць (додати = зміна лише на сервері, без оновлення застосунку).
  - **Застосунок:** `CollectorService` (окремий HttpClient без нашого JWT, gzip, браузерний UA), кнопка «Зібрати ціни зараз» у профілі лише для колектора. Парсингу в застосунку нема.
  - Тести: `test_ingest_html` 8/8 + 15 живих смоук-перевірок ендпойнтів (TestClient, БД-залежність підмінена) + 11 живих у `test_api` (персист-шлях — у CI з postgres:18).
  - ⚠ **Ролі роздаються напряму в БД** (trusted-people): `UPDATE app_user SET role='collector' WHERE lower(email)=...`. Після цього користувач мусить **перезайти** (роль у JWT видається при логіні). Ендпойнта зміни ролі поки нема.
- **✅ ЗАМКНУЛОСЯ НАЖИВО (2026-07-19):** оператор (роль collector, `user_id 3`) натиснув «Зібрати ціни зараз» у застосунку → **54 позиції з 10 сторінок** (1 хаб + 9 лендингів) Allo зайшли з його резидентного IP через `/api/ingest/html`; сервер розпарсив і зберіг; видно у стрічці hapay.today (Samsung Galaxy з MPN `SM-…` у назвах, old→new ціни). Застосунок реально «тупий фетчер» — парсив сервер.
- **Фікс надійності (коміт `c1b738d`):** профіль тепер звіряється з `/api/me` на відкритті (актуальна роль без ручного перезаходу; авто-logout на 401 — застаріла сесія не висить мовчки). Впіймано наживо: кеш показував акаунт, якого не було в поточній БД.
- ⚠ Прод-акаунти-сміття: `deploy-check@` (user_id 2) + `diag-form-20260719@` (user_id 4, мій діагностичний) — оператору видалити: `DELETE FROM app_user WHERE user_id IN (2,4);`. Робочий колектор — `user_id 3` maindevtools@gmail.com.
- **Далі:** задача 3 «покращити UI» (лоск карток, іконка/сплеш «Хапай», фікс обрізання селектора категорій); автоматизувати збір (розклад/фоновий тригер у застосунку чи cron у оператора); адаптери Rozetka/Foxtrot/Moyo під html-ingest (додати до HTML_SOURCES, коли будуть серверні адаптери).

**Наступний реальний крок (за спаданням цінності):**
1. **Автоматизувати сателіт** оператора (Task Scheduler/cron, `collect --satellite` 1-2×/добу) — щоб дім збирав сам.
2. **Адаптери Foxtrot/Moyo/Eldorado** під сателіт-Actions (Azure їх пускає) + **матчинг по MPN** (той самий телефон у ≥2 крамницях; ⚠ пастка суфіксів AUXUA).
3. **Перевдягти UI під T14** (агрегатор, не верифікатор); фікс обрізання селектора категорій.
4. Юр-сторінки на hapay.today (privacy/support для сторів), далі застосунок (S8).

**Заблоковано / чекає власника:** O2–O9 + `BACKUP_TARGET` (офсайт-бекап — коли назбирається тиждень історії).

> **CI:** jobs `tests`+`migration` (постгрес 18) зелені; `docker`-джоб знято разом із Dockerfile (прод bare-metal). Деплой-патерн: пуш у main → на сервері `git pull && bash deploy/hetzner/setup.sh` (ідемпотентний) або точкові `systemctl restart`.

---

## Журнал сесій

> Append-only. Shutdown-ритуал дописує рядок: дата · режим · що зроблено · де спинились · наступний крок. Найновіше зверху.

| Дата | Режим | Що зроблено | Наступний крок |
|---|---|---|---|
| 2026-07-20 | **Виконавець** | **Адаптер Eldorado — 9-те джерело (б'є в справжнє вузьке місце).** Розвідку робив у справжньому браузері, бо SPA. URL знову НЕ вгадувались: `/smartfony/c1050/` дав сторінку помилки, справжні знайшлись у навігації розділів — смартфони `/uk/smartphones/c1038946/`, ноутбуки `/uk/notebooks/c1039096/`, ТВ `/uk/led/c1038962/`; пагінація `page=N/` (з `rel=next`). Перевірено парсингом: смартфони 32 картки/11 із цінами (решта — «Продано»/«Незабаром», їх пропускаємо), ноутбуки 40/32, **ТВ 40/40**. Розмітка: хешовані styled-components → селектори лише ПРЕФІКСНІ (`Pricestyled__StyledCurrentPrice`), назва з атрибута `title`, ключ = id товару з `/pNNN/` (не слаг і не мовний префікс). **Головна пастка:** поруч із ціною лежить блок розстрочки «від 1 200 грн.» — наш `parse_price_to_kop` зчитав би його як 1200, тому ціну беремо ТІЛЬКИ з власного вузла ціни; на це є окремий тест. Фото в лістингу немає (малює скрипт) → `image_url=None`. Касета — з РЕАЛЬНОЇ відрендереної сторінки, 3 картки: зі знижкою, звичайна (з пасткою) і «Продано». 7 golden-тестів, 178 юніт зелені. Черга: 189 сторінок, 15.8 оренд/год проти 20 у телефона (запас 1.27×) — тому Eldorado поставлено 5 сторінок, а не 10. **Це ще й перша бойова перевірка скрол-рендерера:** без нього Eldorado віддав би товари з нулем цін. **✅ ПІДТВЕРДЖЕНО НАЖИВО:** Eldorado заїхав — 9 смартфонів, серед них саме той `OPPO Reno 12 FS 9 999/10 999`, що був у розвідці й касеті (ланцюг замкнувся від браузера до бази). **Це і є доказ, що скрол у нашому офскрін-WebView працює**: без прокрутки Eldorado віддає товари з нулем цін, а адаптер позицію без ціни відкидає, тож було б рівно 0. Гіпотеза, яку я на Brain помилково прийняв без перевірки, тут нарешті стала виміряним фактом. Загалом за сесію: карток 2905→4638, крос-крамничних груп 486→**864**. | Дати черзі добу: у Eldorado відпрацювала 1 задача з 15 (ноутбуки/ТВ ще попереду, а там 32/40 позицій із цінами проти 11 у смартфонів). Далі: підняти Eldorado до 10 сторінок, якщо черга встигає; пагінація Brain (клік по AJAX-пейджеру); нові категорії. |
| 2026-07-20 | **Виконавець** | **Скрол у WebView-рендерері (знімає спільну перешкоду).** Діагноз даними: 82% товарів лежать в ОДНІЙ крамниці (2253 з 2740), тобто порівняння працює лише для 18%; і глибина цьому не лікує — при 5 сторінках у ТВ порівнювалось 27%, при 10 стало 18% (далі перших сторінок іде хвіст, унікальний для кожної крамниці). Отже важіль — БІЛЬШЕ КРАМНИЦЬ. Перевірив Eldorado у браузері: URL довелось шукати в навігації (вгаданий `/smartfony/c1050/` → сторінка помилки; справжній — `/uk/smartphones/c1038946/`), і головне — **без скролу він віддає 32 товари з НУЛЕМ цін, після прокрутки зʼявляється 30 цінників**. Той самий скрол потрібен Brain (нескінченна стрічка — єдиний лишився без пагінації, 37 товарів проти сотень в інших) і прибирає давню болячку «перший render-прохід дає 0». Рендерер переписано: замість «чекати 4 с і зняти HTML» — прокрутка з подією `scroll` до **стабілізації DOM** (розмір outerHTML не змінився 2 рази поспіль; стеля 12 кроків, таймаут 35→60 с). Наслідки враховано: `_MAX_HTML` 5→12 МБ (нескінченна стрічка віддає більший HTML) і таймаут ApiService 20→60 с (тіло тепер кілька МБ). 171 юніт зелений. Зібрано й розгорнуто (0 помилок), регресу нема — збір триває (Comfy 210→253). **❌ ГІПОТЕЗА ПРО BRAIN ВИЯВИЛАСЬ ХИБНОЮ, і це головний висновок.** Я вирішив, що Brain — нескінченна стрічка, бо не знайшов посилань на сторінки; перевірив у браузері вже ПІСЛЯ реалізації: його лістинг рендерить **24 унікальні товари** (96 елементів `data-name-ua` — по 4 на товар) і прокрутка до самого низу не додає ЖОДНОГО; при цьому каталог заявляє 2305 ноутбуків. Справжня причина — **пагінація на JavaScript**: контроли «1», «2» існують, але з ПОРОЖНІМ `href` (AJAX), тому й `page=2/`→404. Тобто Brain потребує КЛІКУ по пейджеру у WebView, а не скролу; ми з нього вже беремо майже все, що сторінка дає (21 із 24). **Отже цінність скролу тримається лише на Eldorado** — там докази прямі й виміряні (без скролу 32 товари з 0 цін, після — 30 цінників), але в НАШОМУ рендерері вона поки НЕ підтверджена, бо адаптера Eldorado ще нема. Урок: механізм довантаження треба перевіряти в браузері ДО реалізації, а не виводити з відсутності посилань. | **Адаптер Eldorado** — він і додасть крамницю (реальне вузьке місце: 82% товарів в одній крамниці), і водночас перевірить скрол у бою. Brain-пагінація (клік по AJAX-пейджеру) — окремо й потім, він дрібний внесок. |
| 2026-07-20 | **Виконавець** | **Пагінація 5 → 10 сторінок (174 лістинги) + оренда 3→5 у застосунку.** Глибину перевірив фактом: стор. 8/10/12 віддають повні набори без повторів 1-ї (Foxtrot/Moyo/Rozetka/Citrus). **KTC — лише 7**: далі його лістинги віддають порожньо, тож слати запити немає сенсу. Головне — не сама цифра, а **пропускна здатність**: 174 задачі / 12 год ≈ 14.6 оренд/год, а телефон при 3 задачах за прохід давав лише 12/год → черга відставала б. Тому підняв оренду до 5 (стеля `MAX_LEASE`=20) → 20/год, запас 1.4×. Розліт 15 хв/крамниця тримає ввічливість: ~15 запитів/год на всі 8 разом. Синхронізацію MAUI робив **власним `sync-maui.ps1`** — і він одразу показав свій дефект: `Where-Object` з одним результатом повертає скаляр, тож `.Count` друкував порожнє; полагоджено через `@()`. **✅ Перевірено наживо:** застосунок тягне `rozetka.com.ua/.../c80003/page=…` (пагінована адреса), прохід триває >4 хв замість ~1.5 (5 задач); за час перевірки карток 2269→2736, груп 401→486 і далі росте. | Дати черзі добу на повний цикл. Далі: категорії (планшети/годинники/консолі), скрол Brain у WebView, або зіставлення ноутбуків. |
| 2026-07-20 | **Виконавець** | **Пагінація: 21 → 93 лістинги (≈5× даних).** Схеми НЕ вгадував: витяг із навігації крамниць (`rel=next`, посилання-номери), тоді перевірив ФАКТОМ — сторінка 2 мусить дати ІНШІ товари. Перетин зі сторінкою 1 усюди **0**: Foxtrot/Moyo/KTC/Citrus `?page=N`, Comfy `?p=N`, Rozetka `page=N/`. **Brain — без пагінації** (перевірено у браузері: посилань нема, нескінченний скрол; `page=2/`→404, `?page=2`→200 але без товарів у HTML; потрібен скрол у WebView — окрема робота). Реалізація ключова тим, що **категорія успадковується**: `source_listings(cfg)` розгортає лістинг у сторінки 2..N на рівні реєстру, тож `URL_CATEGORY`, `collect_plan` і `seed_tasks` беруть їх звідти — без колонки в БД і без міграції (інакше товари з 2-ї сторінки тихо падали б у «Інше»). Пропускна здатність перевірена рахунком: розліт 15 хв/крамниця → 4 задачі/год на крамницю, пагінованій треба 1.25/год (запас 3×); разом 93 задачі/12 год ≈ 7.8 оренд/год проти 12/год у телефона (запас 1.5×). Навантаження на крамниці лишається делікатним: ~8 запитів/год на всі 8 разом. +4 тести (успадкування категорії, схема per-store, Brain без пагінації, усі сторінки зареєстровані). 171 юніт зелений. | Деплой; черга досіває нові задачі сама (`seed_tasks` ідемпотентний), застосунок оновлювати НЕ треба. Повний цикл ~4 год замість ~1. Якщо черга відставатиме — підняти оренду в застосунку з 3 до 5 за прохід. |
| 2026-07-20 | **Виконавець** | **Матчинг інлайн-артикулів — ТВ/ноутбуки нарешті зіставляються.** Діагноз вимірюванням: після наповнення ноутбуками/ТВ частка товарів у 2+ крамницях була смартфони 22%, ноутбуки 5%, **ТВ 0.4%** — тобто вся цінність агрегатора для них не вмикалась. Причина: `extract_mpn` знав лише `SM-` і токен У ДУЖКАХ, а ТВ пишуть артикул інлайн («LG 43UA75006LA»); доказ — той самий LG лежав 4 окремими картками по 16 499 ₴ у 4 крамницях. Додано: (1) крапка в дужковому шаблоні — Acer `(NX.EL5EU.003)` не ловився зовсім; (2) ІНЛАЙН-правило зі суворішими фільтрами (лише ВЕЛИКІ, ≥8 символів, ≥2 літери І ≥2 цифри) — довжина 8 навмисна, відсікає серію без конфігурації («65B5», «X1504VA»), на якій і стається перезлиття. Тести спіймали 2 справжні дефекти первинної версії: інлайн почав хапати короткий `SM-S942B` (те саме перезлиття, від якого модуль захищався) → SM-токени інлайну заборонено; і «найдовший» кандидат брав модельне позначення замість заводського коду → замір на 371 назві показав, що кандидатів кілька лише в 7 і в усіх 7 правильний ОСТАННІЙ (він же збігається з тим, що інші крамниці пишуть у дужках) → беремо останній. **Результат на реальних 730 назвах із прода:** ТВ артикул 26→190, крос-груп **0→36**; ноутбуки 158→224, груп 0→6; смартфони — байт-у-байт без змін (нуль регресу); підозрілих злиттів (розкид цін >35%) немає — у знайдених групах ціни збігаються до гривні. Зоо не зачепило. +11 golden-тестів на реальних назвах, 167 юніт зелені. Додано `scripts/backfill_mpn.py` — разовий перерахунок mpn наявним товарам (інакше згрупуються лише через ~12 год, при наступному зборі). **✅ ПІДТВЕРДЖЕНО НАЖИВО:** бекфіл оновив 115 рядків, груп у базі 109; LG 43UA75006LA став ОДНІЄЮ карткою з `offers_n=4` (Citrus/KTC/Moyo/Rozetka по 16 499 ₴) замість чотирьох окремих; ТВ у 2+ крамницях **0.4% → 21%** (як у смартфонів), карток ТВ 238→177 (дублікати згорнулись); аудит усіх 110 прод-груп — **0 з розкидом цін >35%**, тобто хибних злиттів немає. ⚠ Дорогою: перші два запуски бекфілу нічого не дали, бо виконувався `--dry-run` (я дав його першим у прикладі), а ще раніше — крихка порада діставати DATABASE_URL через `grep\|cut` рвала пароль зі знаком «=»; полагоджено `--env-file`. | Далі на вибір: ще категорії (планшети/годинники/консолі — механізм готовий), пагінація (зараз ~50 позицій із тисяч), або підняти зіставлення ноутбуків (6% — там артикул часто лише в одній із крамниць). |
| 2026-07-20 | **Виконавець** | **Comfy переведено на `mode="render"` (рішення оператора) + його ноутбуки/ТВ.** Гіпотезу про блок перевірив у СПРАВЖНЬОМУ браузері (Claude Browser MCP, як колись Brain): заглушки немає, сторінки віддають по 50 карток. Строга перевірка — не «є ціни на екрані», а чи читають саме НАШІ селектори: `div.product-tile-catalog` + `.product-tile-title`/`.product-tile-price__current`/`.product-tile-price__old-value`/`img` → **50 із 50 карток парсяться**, з поточною й старою ціною та https-фото. Тож адаптер міняти не довелось — досить режиму рендеру. Розмір рендеру 1.9–2.4 МБ, під `_MAX_HTML` (5 МБ) — інджест прийме. Додано `comfy.ua/notebook/` і `comfy.ua/flat-tvs/`. Разом: **21 лістинг, симетрично 7 smartfony / 7 noutbuky / 7 tv**; render-цілей 6 (3 Comfy + 3 Brain). 157 юніт зелені. | Деплой сервера. Телефон має рендерити Comfy у WebView — перевірити, що Comfy знову тече (перший прохід render інколи дає 0, якщо JS не встиг — як було з Brain). |
| 2026-07-20 | **Виконавець** | **Фаза 2 наповнення: +ноутбуки та ТВ (12 лістингів, 6 крамниць).** URL НЕ вгадував — витяг із навігації самих крамниць (розвідка по головних сторінках), тоді **перевірив парсингом адаптера**, а не лише кодом 200: Foxtrot 42+42, Moyo 24+24, Rozetka 60+60, Citrus 47+47, Brain 24+24, KTC 48+48 (усі з цінами, host-політику проходять). `HTML_SOURCES` тепер 19 сторінок + 1 хаб (7 smartfony / 6 noutbuky / 6 tv); черга й сітка каталогу підхоплять самі (seed_tasks ідемпотентний, `/api/categories` віддає лише непорожні). **⚠️ Знахідка: Comfy почав блокувати** — на ВСІ програмні GET (навіть на робочий смартфон-лістинг) віддає анти-бот заглушку «Pardon Our Interruption» (6 КБ, `challenge`). Тому ноутбуки/ТВ для Comfy НЕ додано (нема чим перевірити); ймовірний фікс — `mode="render"` (WebView, як Brain) — чекає рішення. Новий інваріант-тест: кожна категорія з `HTML_SOURCES` мусить бути і засіяна міграцією, і мати розділ+іконку в `CATEGORY_UI` (ловить друкарську помилку в slug, інакше товари тихо падали б у «Інше»); негативну перевірку прогнано. 157 юніт зелені. | Деплой сервера → колектор сам почне тягнути нові лістинги (повний цикл ~3 проходи на крамницю). Тоді в каталозі зʼявляться плитки «Ноутбуки та ПК» і «Телевізори». Рішення 🧭 по Comfy: переводити на render чи лишити. |
| 2026-07-20 | **Виконавець** | **⚠️ ЗНАЙДЕНО КОРІНЬ «зміни не застосовуються» + УСЕ ПЕРЕВІРЕНО НАЖИВО.** Оператор двічі бачив «повернутий» перемикач і відсутній каталог — я двічі казав «це стара збірка». Причина: **`app-maui/` у репо — лише вихідники, без `.csproj`**; реальний VS-проєкт — `C:\Users\CaveMan\source\repos\Hapay\Hapay\`, і `git pull` його НЕ оновлює (README крок 3 — копіювати вручну; крок випадав). Порівняння хешів: 3 файли (CatalogPage.xaml/.cs, CatalogViewModel.cs) взагалі **були відсутні**, 8 застаріли, решта збігалась байт-у-байт (тобто чисті копії, ручних правок немає). Синхронізував 11 файлів → `dotnet build -f net10.0-android` **succeeded, 0 errors** (перша реальна перевірка, що мій MAUI-код компілюється, включно зі строгим SourceGen-XAML) → розгорнув на емулятор → перевірив скріншотами через adb. **Працює все:** головна-каталог із фото на плитках + «Популярні моделі» («від 25 599 ₴»), розділи Електроніка/Зоотовари, тап→«Смартфони (234)» з передобраною категорією, «Найбільша знижка», без повзунка, «Усі категорії» без «(0)»; картка: діапазон згори, «в 2 крамницях», у «Де купити» по крамниці ціна+`−%`+перекреслена стара (Citrus/KTC 33 999 ₴ −22% / 43 499 ₴). Дрібний фікс за зауваженням: заголовок картки для ГРУПИ був назвою однієї крамниці («Citrus») → тепер «Порівняння цін» (`DetailViewModel.PageTitle`); для однієї крамниці — її назва. Записано в пам'ять сесії. | **Щоразу після правок у `app-maui/` — копіювати у VS-проєкт**, інакше оператор збирає старе. Варто зробити `sync-maui.ps1`. Далі: Фаза 2 наповнення (URL ноутбуків/ТВ у HTML_SOURCES → нові плитки самі зʼявляться). |
| 2026-07-20 | **Виконавець** | **Головна = сітка-каталог (E-Katalog-стиль, Фаза 3).** Рішення оператора: показувати ЛИШЕ категорії зі знижками, порожні ховати; тап → знижкова стрічка категорії. Живі дані показали ДВІ наповнені вертикалі: Електроніка (Смартфони 242/знижок 166, Аудіо, Аксесуари) + Зоотовари (~377 товарів: коти/пси корм/консерви/догляд + амуніція). Макет звірено з оператором (visualize-віджет). Реалізація: **бекенд** — `taxonomy.CATEGORY_UI` (розділ+емодзі-іконка на slug) + `SECTION_ORDER`; `db.categories()` збагачує section/icon і сортує за розділом→к-стю (уже фільтрувала лише знижкові — «порожні не показуй» вже виконано). **MAUI** — нова `CatalogPage`+VM (згрупована CollectionView 2 колонки, плитка іконка+назва+к-сть, тап→OpenCategory), `Category`+section/icon+`CategoryGroup`, `HomeViewModel` став `IQueryAttributable` (приймає Category/Query/Title, пуш із каталогу, PageTitle=назва категорії), AppShell лендинг CatalogPage + HomePage як пуш-маршрут, DI. test_api +section/icon. 155 юніт зелені. | **Деплой сервера** (categories з section/icon) + **перезбірка застосунку**. Далі: наповнити нові категорії (ноутбуки/ТВ — Фаза 2, дописати URL у HTML_SOURCES); іконки/лоск плиток за скрінами. |
| 2026-07-20 | **Виконавець** | **UI-розворот у бік порівняння цін (план оператора, лише MAUI).** (1) Прибрано перемикач «Лише знижки» — стрічка тепер ЗАВЖДИ знижкова (`onlyDiscounts:true`), як ідентичність «Хапай»; лейбл сорту «Спочатку знижки»→«Найбільша знижка». (2) Детальна картка: згори — **ДІАПАЗОН цін по крамницях** (`PriceRangeText` з тих самих оферів, що й «Де купити» → гарантована узгодженість; «5 999 – 6 499 ₴» або одна ціна), під ним «Наявно в N крамницях»; класичне «стара ціна + −%» лишили ЛИШЕ для однієї крамниці (`ShowSingleDiscount`). Суть (оператор): «якщо в іншій крамниці дешевше — навіщо знижка»; `product_offers` уже віддає ЦІНИ ВСІХ крамниць (і без знижки) → блок «Де купити» це вже показує. (3) Фікс «Усі категорії (0)» — синтетичний запис без лічильника. **Бекенд НЕ чіпав** (сервер уже вміє `only_discounts`, офери — всі крамниці). ⚠ Інтерпретація п.1 = «стрічка лише знижкова» (за буквою «завжди зі знижками»); якщо оператор мав на увазі «усі товари, знижку позначати» — фліп у 1 рядку. | Оператор: перезбірка застосунку у VS (деплой сервера НЕ потрібен). Перевірити: стрічка без перемикача, детальна картка з діапазоном + усіма крамницями. Далі: Фаза 2 категорій (URL ноутбуків/ТВ). |
| 2026-07-20 | **Виконавець** | **Категорії — контент-уточнення (кабелі/навушники з Brain у «Смартфонах»).** Наживо після деплою №2: у `smartfony` (231) виявились 10 аксесуарів, УСІ з Brain — його лістинг `Smartfoni_zvyazok-c297` = широкий департамент «Смартфони та зв'язок» (телефони + кабелі + навушники впереміш). Рішення: НЕ міняти Brain-URL, а зробити категоризацію контент-свідомою — тоді широкий лістинг стає перевагою (наповнює 3 категорії коректно). `taxonomy.refine_category(base_slug, title)`: для пристроєвих баз (smartfony/noutbuky/planshety/tv) уточнює за назвою — навушники/гарнітура/airpods→`audio`, кабель/power bank/чохол/стилус→`aksesuary`; високоточно (слова, яких у назві телефона не буває) → реальні телефони не чіпає; зоо/не-пристроєві бази лишає. Застосовано в `persist_items` (base з лістинга → refine за назвою). `0009` — разовий бекфіл наявних 10 (ті самі патерни як ILIKE, forward-only). Тести: +3 у test_taxonomy (кабель→aksesuary, навушники→audio, телефон лишається; зоо не чіпає), test_migration [1..9] + refine-persist + 0009-бекфіл; 155 юніт зелені; Foxtrot-касета (3 телефони) лишається smartfony. | **Деплой №3** (`0009` + код) → перевірити: кабелі/навушники Brain пішли в aksesuary/audio, у «Смартфонах» лише телефони. Далі: Фаза 2 (URL інших категорій per-store). |
| 2026-07-20 | **Виконавець** | **Розбиття на категорії — Фаза 1 (категорія-з-лістинга).** Діагноз наживо (`/api/categories`): усі ~213 товарів електроніки падали в «Інше», бо `taxonomy.py` — зоо-only (`categorize()` для електроніки → `inshe`). Обране рішення: категорію брати **з ЛІСТИНГА, який зібрали** (ми точно знаємо, що тягли смартфон-лістинг), а НЕ вгадувати з product-URL. Вертикаль: `0007` сідить 10 категорій електроніки (smartfony/noutbuky/planshety/tv/audio/…); 7 SSR-лістингів у HTML_SOURCES тепер `(url, "smartfony")`-таплі + Allo (hub) має джерело-рівневий `"category":"smartfony"`; `URL_CATEGORY[(source,url)]` будується з реєстру; `ingest_html` резолвить slug (поіменний тег → hub-дефолт → нема) і передає в `ingest_batch`→`persist_items(category_slug=)`, де фіксований slug **перебиває** `categorize()`. Зворотна сумісність: JSON-`/api/ingest` і зоо-`collect.py` без slug → стара URL-логіка. `collect_plan`/`seed_tasks` розгортають таплі (`_url_cat`). Тести: 152 юніт зелені; test_migration [1..8]+override-перевірка, test_api +«Foxtrot html-лістинг → 3 в smartfony» (БД — у CI). **0008 — разовий бекфіл:** наявні 213 товарів електроніки зібрані ДО 0007 → досі в «Інше» (категорія присвоюється при інджесті); у Фазі 1 усі 8 джерел = смартфон-лістинги, тож `UPDATE … WHERE source IN (8 ел.) AND category=inshe → smartfony` (зоо не чіпає, forward-only, ідемпотентно). CI [1..8] зелений. | **Деплой №2** (`0008`) → перевірити `/api/categories` (smartfony з'явиться, «Інше» впаде). **Фаза 2:** розвідати й дописати URL інших категорій per-store (ноутбуки/ТВ/планшети/навушники) → реальне розбиття. **Фаза 3:** сітка категорій у застосунку (як E-Katalog). |
| 2026-07-20 | **Виконавець** | **Дата кінця акції — ЗНАЙДЕНО шлях і реалізовано.** Розвідка «як витягнути»: Rozetka декларує кінець дії ціни в JSON-LD `"offers":{"priceValidUntil":"2026-07-26"}` — реальні дати для знижок (26.07, за 6 днів), генеричні далекі (2027) для не-знижкових; Comfy/Stylus ставлять «сьогодні» (генерично), решта поля не дають. **Не вигадка — офіційне поле схеми.** Вертикаль: `RawItem.promo_until` → adapters/rozetka (regex по JSON-LD `.{0,400}?` бо `brand{}` між url/offers) → `0006` store_product.promo_until DATE → persist/validate_item (перевіряє формат ISO) → API віддає ЛИШЕ реальну (майбутню й ≤90 днів — `_PROMO_COL` to_char; відсіює 2027/сьогодні) → застосунок «Акція діє до DD.MM». Фікс: `first_seen_at` ambiguous з JOIN sp0 → b.-кваліфікація сортів. test_rozetka +promo, test_migration [1..6], test_api +2 (дати динамічні). CI зелений. Механізм готовий — інші крамниці додамо, коли знайдемо, що дають дату. | Деплой (0006 + код) + перезбірка застосунку. Далі: реліз-трек / ще крамниці / push. |
| 2026-07-20 | **Виконавець** | **«Усі товари» (повний прайс-агрегатор) — за запитом власника.** Стрічка більше не лише знижки: `/api/products` (`list_products` — остання ціна кожного товару DISTINCT ON по `ix_ps_prod_window`, MPN-дедуп, has_discount/offers_n, сорти discount/cheap/expensive/new, `only_discounts=1` звужує). Дані для крамниць-лістингів уже були в `price_snapshot` (збирали всі картки, не лише знижкові) — тепер показуємо. Застосунок: стрічка тягне /products, перемикач «Лише знижки», сорти оновлено. test_api +6, CI зелений. **Час акції — розвідано, чесний вердикт:** реальної дати кінця акції в лістингах НЕМА (Comfy `priceValidUntil` виставлено генерично на сьогодні; Brocard timer=купон). Фейковий відлік порушив би T12/§7.5. Запропоновано чесні альтернативи: «ціна станом на DD.MM» (свіжість, є `seen_day`) та «знижка вже N днів» (`announce_date` — довга «акція» = сигнал накачки). Чекає рішення власника. | Реалізувати обрану чесну дату на картці. Далі: реліз-трек (ФОП/стори), ще крамниці/категорії, push. |
| 2026-07-19 | **Виконавець** | **Адаптер KTC (8-ме джерело) + юр-сторінки (крок до релізу).** Батч-розвідку 8 кандидатів проведено: більшість — JS-головні/SPA/нетипові (park); KTC — чистий SSR (`/smartphone/`, 48 карток, ціна del-cram → `del.decompose()`); S26/A07 перетини. Оператор ухвалив: 8 крамниць достатньо, далі реліз. **Юр-сторінки** (privacy/terms/support) — HTML українською, точні щодо застосунку (мінімум даних, БЕЗ телеметрії §7.7, колектор за згодою, дані в ЄС, видалення через підтримку); `GET /{page}` у main.py (не чіпає /api/*, смоук ✓); посилання в профілі (Launcher→браузер). CI зелений. **⚠ ЧЕРНЕТКА:** оператор має вписати реквізити ФОП, налаштувати support@hapay.today, дати на юр-перегляд. | Реліз-трек 🧭 (рішення власника): ФОП + акаунти розробника (Play $25 / Apple $99), iconи/сплеш готові, iOS-збірка (Mac/Codemagic), заповнити реквізити в юр-сторінках, лістинги в сторах. Опційно: ще крамниці (механізм готовий), push-сповіщення (watchlist є). | 
| 2026-07-19 | **Виконавець** | **WebView-режим збору (SPA-крамниці) + адаптер Brain (7-ме джерело).** Нова можливість: телефон РЕНДЕРИТЬ SPA-сторінку в офскрін-WebView (ціни зʼявляються після JS) і шле готовий HTML — сервер парсить, як завжди. Розвідку Brain робив у РЕАЛЬНОМУ браузері (Claude Browser MCP): підтверджено, що після JS ціни є; дані найнадійніші в data-атрибутах кнопки «Купити» (`data-price`/`data-without-discount-price`/`data-name-ua`), фото в lazy `data-observe-src`. Adapter Brain (golden 6/6, касета = відрендерений HTML), A07 SM-A075FZKGSEK перетин із Moyo/Rozetka/Citrus. Сервер: `mode` per-source (render/fetch) БЕЗ міграції (властивість джерела в коді — COLLECT_MODE); collect_plan/lease віддають mode. Застосунок: `IWebRenderer`+`AndroidWebRenderer` (офскрін-WebView, JS on, settle 4с після OnPageFinished, EvaluateJavascript→outerHTML, таймаут 35с, фото не тягне); CollectorService.GetHtmlAsync маршрутизує render/fetch обидва шляхи. CI зелений (lease Brain=render/Foxtrot=fetch). C# скомпілювався (CLI-білд дійшов до пакування; «помилка» = лок VS на obj, не CS). | Оператор: перезбірка VS + деплой (7 джерел). Тест: фоновий збір має тепер тягнути й Brain (render). Далі: Eldorado через той самий WebView-режим (розвідка + адаптер); ще SSR-крамниці (зоо/б'юті); пагінація; юр-сторінки. **✅ ПІДТВЕРДЖЕНО НАЖИВО 2026-07-19:** усі 7 крамниць збираються (Brain — 10 товарів через WebView-render, не 0!); стрічка — групи до 5 крамниць (Samsung A57: Allo+Citrus+Comfy+Foxtrot+Rozetka по 25 599). Перший прохід render інколи дає 0 (JS не встиг), наступний — ok; за потреби зміцнити рендерер (attach-to-window + чекати стабілізації) — поки не критично. |
| 2026-07-19 | **Виконавець** | **Адаптер Citrus (6-те джерело) + фікс голодування черги.** Розвідка Eldorado/Brain/Citrus: справжні category-URL знайдено з home-сторінок (вгадані 404); **лише Citrus SSR** (Next.js, 47 карток) — Brain/Eldorado рендерять лістинги JS-ом (SPA) → відкладено до WebView-режиму. Citrus-адаптер: `[class*=MainProductCard-module__root]` (хеші `___XXXX` міняються між білдами → ПРЕФІКСНІ селектори), стабільний `.old-price`, фото декод із Next-проксі; golden 6/6; SM-S948BZKGEUC перетин із Comfy. **Важливий фікс (виявлено при 6>MAX_LEASE):** lease-SQL сортував по source (абетка) → нові адаптери (Foxtrot/Moyo/Rozetka) ГОЛОДУВАЛИ б за перших-за-абеткою. Тепер зовнішній `ORDER BY priority, not_before` (найдовше очікувані першими); MAX_LEASE 5→20 (ceiling; застосунок просить 3); +антистарвейшн-тест. CI зелений (кілька ітерацій: хибне очікування «порожньо» при джерелах>стелі, забутий імпорт — виправлено). | Деплой (6 джерел + фікс ротації). Далі: Brain/Eldorado через WebView-режим збору (нова можливість застосунку); пагінація/категорії; зоо/б'юті-адаптери; юр-сторінки. |
| 2026-07-19 | **Виконавець** | **Стрічка стала справді агрегаторною** (зауваження оператора на живих даних: картка групи писала «Foxtrot», хоча телефон у 2 крамницях). `list_discounts` тепер КОЛАПСУЄ по MPN (CTE `best`, DISTINCT ON gkey=COALESCE(mpn,'sp:id')): товар — одна картка, представлена найдешевшою пропозицією; без mpn — кожен сам собі. Прибирає й дублі (той самий телефон по картці на крамницю). Клієнт: група → «від {найдешевша} · Наявно в N крамницях» БЕЗ назви однієї крамниці (PriceText/ShowStoreLine); одна крамниця — як було. CI: S26/OPPO у стрічці один раз, offers_n рахує крамниці, «8 declared» ціле. Раніше цього ж дня: дедуп у product_offers (одна пропозиція/крамницю — фікс «Foxtrot/Foxtrot» на OPPO CPH2801). | ⚠ **Деплой на сервер давно назрів** (0004+0005+адаптери Foxtrot/Moyo/Comfy/Rozetka+дедупи стрічки/офферів): `git pull && deploy.sh`. Емулятор втратив мережу (QEMU) — тест на фізичному телефоні або cold boot. Далі: ще адаптери, пагінація, юр-сторінки. |
| 2026-07-19 | **Виконавець** | **Адаптер Rozetka** (найбільший маркетплейс). Angular-SSR, 60 карток `rz-catalog-tile`, `a.tile-title`+`.price`/`.old-price`, MPN у назві й URL-слагі; ключ = `/pNNN` product-id. golden 5/5, у HTML_SOURCES (5 джерел). **Живий доказ T15 у CI:** інджест Rozetka+Foxtrot лістингів → S26 `SM-S942BZKGEUC` збігається → `/offers` = {Rozetka, Foxtrot} (крос-крамнична група з РЕАЛЬНИХ адаптерів, не синтетика). test_queue зроблено стійким до росту (PER_LEASE=min(джерел,MAX_LEASE), розліт через isdisjoint). Черга-оренда підхопить Rozetka сама. | Ще адаптери з probe (Eldorado/Brain/Citrus/Stylus/KTC — уточнити category-URL; зоо MasterZoo/E-zoo; б'юті). ⚠ **Деплой на сервер давно на часі** (0004 MPN + 0005 черга + адаптери Foxtrot/Moyo/Comfy/Rozetka): `git pull && deploy.sh` — без нього фоновий збір телефона отримує стару HTML_SOURCES (лише Allo) і lease падає (нема 0005). Пагінація/категорії; юр-сторінки. |
| 2026-07-19 | **Виконавець** | **SSR-тест кандидатів + адаптер Comfy.** Ключова знахідка: з РЕЗИДЕНТНОГО IP (те, що бачить телефон) **майже все SSR і з цінами** — навіть Rozetka(JSON-LD!)/Silpo/ATB/Citrus/Allo/MasterZoo/E-zoo, які блокують Azure. Лише EVA 403. Тобто телефон-канал відкриває майже весь ринок. Класифікація ~28 крамниць у `ssr_probe.py` (scratchpad). Написано адаптер **Comfy** (SSR, 50 карток, `.product-tile-price__current/__old-value`, MPN у назвах): golden 5/5, у HTML_SOURCES/INGEST_SOURCES. **Comfy має SM-A376BLVGEUC — той самий A37 Lavender, що в Allo → буде 1-ша реальна група «Де купити» з 2+ крамниць, щойно збір її захопить.** Черга-оренда підхопить Comfy сама (ледачий сів); застосунок НЕ мінявся. test_queue зроблено стійким до к-сті джерел (N_SRC). CI зелений. | Продовжити адаптери з probe-списку: **Rozetka** (найбільший, JSON-LD — найлегший парс), Eldorado/Brain/Citrus/Stylus/KTC (уточнити category-URL — home давав 200, вгадані лістинги 403/404); зоо MasterZoo/E-zoo; б'юті Makeup/Brocard. Кожен = рядок у HTML_SOURCES, черга підхопить. Деплой на сервер (0005 + нові адаптери). |
| 2026-07-19 | **Виконавець** | **✅ T16 ЖИВЕ END-TO-END: фоновий збір працює.** Оператор увімкнув «Збирати автоматично» → WorkManager-прохід сам зібрав 3 стор. (по 1 на Allo/Foxtrot/Moyo — стеля проходу; лічильник у профілі «Сьогодні зібрано: 3 стор.»). Дорогою полагоджено 2 збірні проблеми: (1) minSdk 21→23 (вимога AndroidX Lifecycle від WorkManager; XAAM-помилка маніфесту); (2) D8 «Type defined multiple times» — WorkManager тягнув старі -ktx (core-ktx 1.16 проти core 1.19 MAUI) → явні вирівняні референси в csproj: Core.Core.Ktx 1.19.0.1, Collection.Ktx 1.6.0.1, Lifecycle.Runtime.Ktx.Android 2.11.0.1 (версії звірено з NuGet + project.assets.json). Деплой ганяв з консолі (`dotnet build -t:Run`) — VS ковтав справжню помилку. NU1608 — очікуваний шум. csproj поза git — зміни тримати вручну. | Тримати телефон на зарядці з Wi-Fi уночі → черга сама тече (повний цикл 3 джерел ≈ 3 проходи). Далі: SSR-тест probe-ip кандидатів (Brain/Telemart/Makeup/EVA/…) → нові адаптери → HTML_SOURCES → черга підхопить; пагінація/категорії F/M; юр-сторінки. |
| 2026-07-19 | **Виконавець** | **T16 крок 2 — фоновий збір на Android (коміт `68c8129`).** WorkManager PeriodicWorkRequest 15 хв + констрейнти Wi-Fi(Unmetered)+зарядка (дефолти оператора) — свідомо БЕЗ foreground service (прохід короткий ≤3 сторінки, постійне сповіщення не потрібне; працює й із закритим застосунком). `AndroidCollectScheduler`+`CollectWorker` (Platforms/Android), `RunQueuePassAsync` (lease→fetch→ingest+task_id→fail-бекоф), перемикач «Збирати автоматично» + лічильник «Сьогодні зібрано N стор.» у профілі, EnsureIfEnabled(Keep) на старті. VS csproj (поза git): +Xamarin.AndroidX.Work.Runtime 2.11.2.1 (версію звірено з NuGet). ⚠ Збірку в VS ще не проганяли — байндінг-імена WorkManager можуть потребувати точкового фіксу за компілятором. Сервер у кроці 2 не мінявся. | Оператор: (1) деплой кроку 1, якщо ще ні (`deploy.sh` → 0005); (2) перезібрати VS (NuGet restore підтягне Work.Runtime); (3) увімкнути перемикач у профілі → поставити телефон на зарядку з Wi-Fi → за ~15-30 хв перевірити `GET /api/collect/queue` або лічильник. Далі: SSR-тест ширшого списку (probe-ip кандидати) → нові адаптери → черга сама підхопить. |
| 2026-07-19 | **Виконавець** | **T16 крок 1 — черга-оренда розподіленого збору (рішення власника: усе через телефони).** Спершу перевірено факти: GH Actions безкоштовний для публічних репо (docs), АЛЕ probe-ip з Azure-IP по ~45 крамницях: ~28 пускають (вкл. Brain/Telemart/Makeup/Varus/Antoshka), **~17 блокують — і це найважливіші** (Rozetka/Allo/Silpo/ATB/EVA/Citrus/Kasta…) + ToS-ризик → Actions відкинуто, телефони дістають усе. Побудовано: `0005` collect_task, `api/qtasks.py` (сів з HTML_SOURCES ледачий/ідемпотентний; lease по 1/крамницю з конкурентно-безпечним UPDATE; розліт 15 хв; complete → repeat 720 хв (2×/добу); збій → експ. бекоф; протухання оренди 10 хв; enqueue лендингів хаба в чергу), ендпойнти lease/fail/queue + task_id в ingest/html. test_queue 14/14 живий + 9 ендпойнт-перевірок у test_api; CI ловив хибне очікування ідемпотентності — виправлено; все зелене. Застосунок НЕ мінявся. ⚠ Деплой 0005 — чекає оператора. | **Крок 2:** Android foreground service у застосунку — опитує lease кожні ~15 хв на Wi-Fi+зарядці, тягне задачі, шле ingest/html з task_id; перемикач «збирати» + лічильник у профілі. Потім: SSR-тест ширшого списку крамниць (кандидати з probe-ip) → нові адаптери → HTML_SOURCES → черга сама їх підхопить. |
| 2026-07-19 | **Виконавець** | **Адаптери Foxtrot + Moyo → html-ingest (розкривають T15).** Розвідка з резидентного IP: лістинги категорій обох SSR-лять картки, MPN у назвах; **перетин Foxtrot∩Moyo уже на перших сторінках — 5 товарів** (iPhone 17 Pro MG8J4AF/A, Samsung A16/A07…). Адаптери (Foxtrot: data-атрибути + price--old/current + schema.org фото; Moyo: price_oldprice/current + base64-вказівник фото), обрізані касети + golden 7/7 і 6/6. **Фікс матчингу зі смоуку:** Samsung — найдовший збіг (Foxtrot пише коротку лінійку SM-S942B інлайн + повний артикул у дужках; коротка спільна для варіантів = перезлиття), короткий без повного → None (14/14). HTML_SOURCES += обидва (kind=page) — **застосунок підхопить без оновлення** (сервер = авторитет плану). collect.py свідомо не чіпав (скрипт-сателіт не використовується; test_collect force-вмикає всі джерела). CI зелений: лістинги → 3+3 прийнято, offers-група/AUXUA — усе живе. ⚠ Деплой — чекає оператора. | Оператор: deploy.sh → «Зібрати ціни» в застосунку (тепер потягне Allo+Foxtrot+Moyo) → перші агрегатні групи наживо → перевірити блок «Де купити» на iPhone/Samsung. Далі: більше категорій F/M (дописати URL у HTML_SOURCES), Rozetka-адаптер, юр-сторінки, автоматизація збору. |
| 2026-07-19 | **Виконавець** | **T15 — агрегатна картка за MPN (рішення власника по §17.5) + добита задача 3.** Матчинг: `matching.extract_mpn` (Samsung SM-… будь-де; родовий токен лише в дужках, форм-фільтри проти 256GB/EAN/стоп-слів; ПОВНИЙ ключ — AUXUA не зливається; 12/12), `0004_mpn.sql` (`store_product.mpn` + частковий індекс; бекфіл = колектор сам, upsert 2×/добу), persist пише mpn, `/api/product/{id}/offers` (остання ціна кожної крамниці з сирого snapshot, від найдешевшої; без mpn → []). Застосунок: блок «Де купити» в картці (видно при ≥2 крамницях, тап → крамниця, «немає» приглушено). CI зелений: міграції 0001…0004, група 1→2 крамниці, сортування, AUXUA-тест, зоо → []. Задача 3 добита: укр. формат цін (17 099 ₴ зам. 17,099.00), селектор категорій на повну ширину, картки з рамкою фото. ⚠ Деплой 0004 на сервер — ЧЕКАЄ оператора (`deploy.sh`); mpn наповниться наступним збором (зоо — NULL, Allo — після наступного натискання «Зібрати ціни»). | Оператору: deploy.sh + перезбірка VS; щоб ПОБАЧИТИ групу — потрібна 2-га крамниця з тим самим MPN (Foxtrot/Moyo адаптер або тест-ingest). Далі: адаптери Foxtrot/Moyo під html-ingest (розкриють цінність T15), юр-сторінки, автоматизація збору. |
| 2026-07-19 | **Виконавець** | **Задача 3 (частково): брендування + фільтр ціни — ЖИВЕ.** Розділ 17 (аналіз конкурентів: Rozetka/Hotline/E-Katalog наживо, IA + «що беремо»; тензія 🧭 агрегатна картка ⇄ MPN-матчинг §14.2). Фільтр ціни: `/api/discounts?price_min/price_max` (копійки) + пікер пресетів у застосунку; спершу «не працював» — бекенд не був задеплоєний (сервер мовчки ігнорував параметри), після deploy.sh перевірено наживо (0 позицій поза межею). Іконка+сплеш «Хапай»: біла зубчаста печатка (16 зубців) + червона обводка + червоний % на фіолетовому, суто заливки; v2 різалась adaptive-маскою (~72% пів-осі, артефактний кант) → v3 = 52% пів-осі; «2 кола» на емуляторі = кеш лаунчера (артефакти збірки перевірено чисті) → uninstall+cold boot вилікував. ApplicationTitle=«Хапай» (csproj поза git). Оператор підтвердив: усе працює. | Лишок задачі 3: лоск карток стрічки + фікс обрізання селектора категорій. Далі: 🧭 рішення по 17.5 (агрегатна картка по MPN); юр-сторінки; сателіт-автоматизація. |
| 2026-07-19 | **Виконавець** | **S11 етап 3 — html-ingest + збір із застосунку** (коміти `fc4fce9`+`0a72617`). Бекенд: `require_collector` приймає токен АБО JWT-роль collector+; `/api/ingest/html` (застосунок шле сирий HTML → **сервер парсить** адаптером, двофазно для hub через `discover()`); `/api/collect/plan` (сервер — авторитет над списком). Реєстр `HTML_SOURCES` (Allo). Тести: test_ingest_html 8/8 + 15 смоук-перевірок ендпойнтів (TestClient, БД підмінена) + 11 живих у test_api (персист — у CI). Застосунок: `CollectorService` (окремий HttpClient без JWT, gzip, браузерний UA, fetch→forward), кнопка «Зібрати ціни» у профілі для колектора, `CanExecute`-захист. 6 файлів у VS-проєкт. selectolax уже в спільному venv — деплой ОК. | Деплой бекенду (`git pull && deploy.sh`); власнику: `UPDATE app_user SET role='collector'` + перезайти в застосунку → натиснути «Зібрати ціни». Далі задача 3 (лоск UI, іконка/сплеш, фікс селектора категорій). |
| 2026-07-19 | **Виконавець** | **S11 авторизація, етапи 1-2.** Етап 1 (бекенд, live на hapay.today): `0003_auth.sql` (app_user+ролі, watchlist на user_id), `api/auth.py` — pbkdf2_sha256(600k)+HS256 JWT чистим stdlib, JWT_SECRET з env fail-loud; register/login/me/watchlist; test_auth 11/11 + 9 живих; dbguard/test_migration оновлено (CI зелений). `deploy/hetzner/deploy.sh` (git pull→pip→міграції→рестарт→health — бо 0003 забули після pull). Етап 2 (клієнт MAUI, коміт `9d04b9e`): AuthService(SecureStorage)+ApiService(Bearer)+Login/Profile VM+сторінки, кнопка «Акаунт». Адверсарне рев'ю 4-лінзи → 1 підтв. баг: LoadAsync ловить збій розшифрування SecureStorage (інакше краш-цикл старту) + IsCollector через NotifyPropertyChangedFor. 13 файлів у VS-проєкт. | Оператору: зібрати в VS + прогнати логін на емуляторі; видалити тест-акаунт deploy-check. Далі етап 3: гейт ролі collector на /api/ingest + /api/ingest/html (сервер парсить) + fetch-and-forward у застосунку. Task 3: лоск UI. |
| 2026-07-18 | **Виконавець** | **S8 ЗАСТОСУНОК ЖИВИЙ на Android.** Стек: .NET MAUI/C# (власник обрав проти рекоменд. Flutter — VS уже стояв; аналіз 5-лінз рекомендував Flutter, дисент збережено). MVVM+CommunityToolkit.Mvvm, вбудований фото-кеш, графік історії свій на GraphicsView. Написано → адверсарне рев'ю (6 агентів, 1 minor фікс — «Усі категорії» до запиту) → перенесено в VS-проект `Hapay` (C:\Users\CaveMan\source\repos\Hapay, net10.0-android) → **зібралось і запустилось на емуляторі БЕЗ помилок**. Живі дані з hapay.today: стрічка(фото+ціни+−%), категорії(лічильники), пошук, картка з чесним графіком («Замало вимірів» на бідній історії — фікс із рев'ю працює). Flutter-версія лишилась у app/ (git-історія). | Іконка/сплеш «Хапай»; iOS-збірка (Mac/Codemagic); watchlist+push; юр-сторінки+ФОП для сторів (Фаза релізу). |
| 2026-07-18 | **Виконавець** | **S10 distributed-збір ЗАМКНУВСЯ end-to-end.** POST /api/ingest (токен per-колектор INGEST_TOKENS + серверна валідація кожного елемента: URL на домені крамниці, ціна в діапазоні, інʼєкція відсіюється — test_ingest 12/12, +6 живих у test_api). collect --satellite (ядро fetch+extract винесено; шле payload на ingest, БД не чіпає — test_satellite 5/5). **Живий доказ:** оператор запустив сателіт зі СВОГО дому → Allo (403 будь-якому ДЦ) зайшов з резидентного IP → сервер accepted 48/0, collector 'phone', events 346. Allo-товари (з MPN SM-… у назвах!) видно у вітрині поряд із зоо. Мапа точок: сервер(Hetzner)=Pethouse/PetChoice; дім(резидент)=Allo/Rozetka/EVA; Actions(Azure)=Foxtrot/Moyo/Eldorado. | Автоматизувати сателіт (cron/Task Scheduler у оператора); адаптери Foxtrot/Moyo/Eldorado під сателіт-Actions; **перевдягти UI під T14** (зараз каже «заявлено·не перевірено» / «проти історії цін» — стара модель B, а ухвалено агрегатор); фікс обрізання селектора категорій. |
| 2026-07-18 | **Виконавець** | **S9 Allo-адаптер + мапа ДЦ-фільтрів.** Адаптер Allo (SSR-лендинги акцій, hub-discovery у collect, golden 9/9, CI-інтеграція 11/11) — але **Allo 403 з БУДЬ-ЯКОГО ДЦ** (Hetzner і Azure) → enabled=False, чесно вимкнено. Пробій 34 крамниць із сервера: **11×403 (CDN фільтрують Hetzner), 0 чистих кандидатів A**; пробій з Actions-IP: Rozetka/Allo/EVA/MasterZoo/Watsons/Answear блокують і Azure, **АЛЕ Foxtrot/Moyo/Eldorado з Actions = 200** (з Hetzner — 403!). Висновок: сателіт-колектор (Actions → ingest-API) відкриває реальний шмат техніки. | **S10:** ingest-API (bearer) + collect-сателіт в Actions → Foxtrot/Moyo/Eldorado; полагодити probe_wide (двоступеневий діскавері замість вгаданих URL); 🟡 Eldorado/27.ua з сервера — глянути глибше. |
| 2026-07-18 | **Виконавець** | **🚀 ГО-ЛАЙВ на власному залізі.** Назва «Хапай» + домен hapay.today (T14). Новий GitHub `MainDevTools/hapay` (публічний, повна історія). Hetzner CX23 → bare-metal (Docker відкинуто власником): Postgres 18 (PGDG, localhost-only) + venv/uvicorn + Caddy TLS + systemd-таймери collect/backup. Дорогою: postgres:18-том, /data-лог Caddy, dubious ownership, least-privilege env для Caddy — усе полагоджено. Перший збір: **379 позицій / 309 подій**, https://hapay.today/api/health = ok. | Адаптери електроніки + MPN-матчинг; 🧭 T14-суть; BACKUP_TARGET; юр-сторінки. |
| 2026-07-17 | **Виконавець** | **UI-лоск Mini App** (`web/index.html`): центрована колонка 540px (десктоп), поліровані картки (фото+рамка, червоні «−%», кольорові бейджі з крапкою), skeleton-завантаження, стани порожньо/помилка з іконками, градієнт-графік із точками, grip+CTA у панелі історії, вивірені light/dark, escape+alt. Функціонал незмінний. Оператор схвалив на скріншотах. | Опційно: юніт-ціна грн/кг, лічильник позицій; деплой презентації; headless. |
| 2026-07-16 | **Виконавець** | **Mini App: пошук + пагінація** — аудит-прогалина #3. `/api/discounts` +`q` (ILIKE §9.1); web: search-інпут (debounce) + «Показати ще» (append), `load(reset/append)`. test_api +3 (пошук/порожній/page=1) → read-API 13/13 проти PG18. **Усі 3 аудит-прогалини закрито.** | Опційно: деплой презентації (Fly/Render), ще покриття (headless), push-бот. Ядро+вітрина — feature-complete для MVP. |
| 2026-07-16 | **Виконавець** | **Категорії (§2.6) + закриття подій (§5.5)** — аудит-прогалини #1/#2. `taxonomy.py`+`0002_taxonomy.sql` (категоризація за URL), `persist_items` категорія на товар, `close_absent` (закриває зниклих >26г), `/api/categories` з лічильниками + селектор у Mini App. CI: +test_taxonomy 5/5, DB-тести оновлено (категорія/close_absent/фільтр) — усе зелене проти PG18. Застосовано до живого Neon (collect: перекатегоризовано 380 товарів). | Аудит-прогалина #3: пагінація+пошук у Mini App; далі покриття/деплой. |
| 2026-07-16 | **Виконавець** | **🚀 ГО-ЛАЙВ на Neon (free).** Neon-адаптація (`0001` plain PG, T11, v2.1) + CI на `postgres:18`. Оператор створив Neon-проект + `DATABASE_URL` secret; я запустив `collect` через `gh` → success: **380 снапшотів / 311 declared-подій** реальних даних Pethouse+PetChoice. Cron 2×/добу активний. | Чекати ~30 днів історії → verified-бейджі (S0.3); опційно деплой read-API+бот (`docs/deploy.md` §2–3), щоб бачити в Telegram. |
| 2026-07-16 | **Виконавець** | **S6-G розширення покриття.** Розвідка нових SSR-крамниць (Foxtrot/Moyo/Citrus/E-Zoo/…): **нових чистих тир-A SSR нема** (Foxtrot=SPA, Citrus=кампанійний індекс, решта 404/JS) — ринок у client-side. Натомість **6 категорій Pethouse** (suhoi-korm/konservi/shampuni × кот/пес, ~303 варіанти vs 84) у `collect.SOURCES`. `collect()` +`delay`-параметр. | Го-лайв; push-бот; або headless для JS-крамниць (велике покриття). |
| 2026-07-16 | **Виконавець** | **S6 адаптери + розвідка E.** ✅ **PetChoice** (miniShop2, тир A) — `test_petchoice` 5/5, у collect (2 джерела, CI 12/11). ⏸ **Horoshop — розвідку XHR завершено, вирок: headless-only.** Браузер-аналіз: MasterZoo тягне ціни зовн. Vercel-сервісом (`product-import-lilac.vercel.app`) + мерж, Petmarket теж client-side (0 цін у DOM). Чистого catalog-XHR нема → потрібен headless (§4.7, M2+). MasterZoo-Vercel-хак не будуємо (крихкий/не-генерик). §3.3/§3.6 оновлено. | Push-бот (§9.3), закриття подій, таксономія; Horoshop = headless-під-проєкт (стратегічне рішення). |
| 2026-07-16 | **Виконавець** | **Деплой-артефакти read-API.** `Dockerfile`+`docker-entrypoint.sh`(опц.міграції)+`.dockerignore`, `fly.toml` (Fly, waw, auto-stop, health), `docs/deploy.md` (БД+collect-secret+Fly/Railway+BotFather), `.gitattributes` (*.sh eol=lf). CI job `docker`: build+smoke `/api/health` → `{"ok":true}` ✅. | Го-лайв — кроки оператора (`docs/deploy.md`): Timescale Cloud + Fly deploy + бот. |
| 2026-07-16 | **Виконавець** | **S5 read-API + Mini App (ВЕРИФІКОВАНО).** `api/` (FastAPI: /discounts category-first+фільтр+сорт, /product/{id}/history, /categories, watchlist за initData-гейтом), `web/index.html` (Mini App: вітрина+бейджі+hotlink-фото+inline-SVG графік, Telegram SDK+тема). CI run 29498156837: initData 5/5 + API 9/9 live. **Уся §8.1 архітектура готова.** | Го-лайв (провізія БД + деплой API + бот) — оператор; або ще адаптери/push. |
| 2026-07-16 | **Виконавець** | **S4 колектор (ВЕРИФІКОВАНО).** `collect.py` (fetch→extract→persist→detect_pass, DI-fetcher), `collect.yml` (scheduled 2×/добу). CI run 29497364326: collect 4/4 на касеті проти живого Timescale (9 снапшотів, 8 declared). Конвеєр запускний. | **Go-live:** оператор провізіонить Timescale Cloud + `DATABASE_URL` secret → реальна історія. Або read-API/Mini App, ще адаптери. |
| 2026-07-16 | **Виконавець** | **S3 `detect_pass` (ядро §5).** `detection/core.py` (чиста логіка: Стадія A+B, 5 станів бейджа, pct ROUND_HALF_UP, OOS-виключення) — 7/7 синтетика; `detection/runner.py` (upsert `discount_event`, подія лише для реальної знижки, announce заморожено §8.4). CI-тест `detect_pass` verified+ідемпотентність. Конвеєр сторінка→RawItem→snapshot→бейдж повний. | Перевірити CI-detect_pass зелений; далі колектор→постійна БД для реальної історії, або read-API/закриття подій. |
| 2026-07-16 | **Виконавець** | **S1 ВЕРИФІКОВАНО ✅** — запуш на GitHub (приватний), `gh` авторизовано, CI job `migration` прогнав `0001` проти живого Timescale: **10/10 DoD** (hypertable/cagg/tsvector/append-only/round-trip), 0 правок. Блокер знято через CI. | **S3 `detect_pass`** (бейджі §5) поверх наявних схеми+персисту. |
| 2026-07-16 | **Виконавець** | **S1 `0001` схема + DB-пул (code-complete).** `migrations/0001_init.sql` (уся §6: hypertable/cagg/компресія/tsvector/append-only тригер/BIGINT), `db/pool.py`+`migrate.py`+`store.py`, skip-aware `test_migration` (DoD §6.6 + round-trip RawItem). requirements+psycopg. **БЛОКЕР:** нема локального Timescale (нема Docker/WSL) → верифікація в CI (додав job `migration` з timescale-сервісом). Локально 18/18, migration skip. | **Пуш на GitHub** → CI прожене `0001` живцем (або зовн. Timescale). Потім правки за першим живим прогоном, далі S3 `detect_pass`. |
| 2026-07-16 | **Виконавець** | **S2 адаптер Pethouse (екстрактор на касеті).** `adapters/base.py`+`pethouse.py` (selectolax), обрізана реальна касета, `test_pethouse` 9/9, `requirements.txt`, CI-гейт `tests.yml` (18/18). Reality-фідбек: картки **мультиваріантні** → RawItem/варіант, `external_ref=url#v=<варіант>` (§4.8/GAP15); вписано §3.3. S2→`in-review`. | Рев'ю S2; далі **S1 `0001` схема + DB-пул** для персисту RawItem, тоді `detect_pass`. |
| 2026-07-16 | **Виконавець** | **Старт розробки (S0.1).** `git init`+перший коміт `d2c46b3`. `probe.py` (stdlib, сигнал-орієнтований) + тести 9/9 + CI-cron. Живий прогін 6 крамниць: **Pethouse+PetChoice працюють** (тир A, old=168/74), решта — тир C/лендинг. Реальність-фідбек у §3.3+Outcome: Pethouse редизайн, калібровано хибні anti-bot, #101 ціни cookie-стабільні. S0.1 → `in-review`. | Рев'ю S0.1 (Диригент); далі S2 адаптер Pethouse — але під **нові** селектори (line-through/price-field), не застарілі §3.3. |
| 2026-07-16 | Диригент | **§8.2/8.5/8.6 переписано central-native** (не banner-deprecated): §8.2 — GH Actions cron + concurrency-group, майстер-розклад у БД, «тиха смерть» через failed-run сигнал, довіра до годинника; §8.5 — deploy-bootstrap (провізія БД→міграції→сід) + Telegram-онбординг без акаунтів; §8.6 — PITR+`pg_dump`+перевірка-відновленням, Timescale-компресія, реконсиляція орфан-scan_run. Топологічно-нейтральні інваріанти (worker-queue/per-host/буфер→txn) збережено. §00-changelog оновлено. | `git init`+S0.1 (Worker) — канон готовий; **АБО** дрібний хвіст: §7.7/§10 desktop-privacy у напрузі з central-only (радше O5). |
| 2026-07-16 | Диригент | **T10 — двигун БД SQLite→PostgreSQL 16 + TimescaleDB, central-only (O1 закрито).** Рішення власника проти рекомендації CC. Переписано канон: `06` повністю (hypertable/cagg/компресія/tsvector/BIGINT/форвардні міграції), `08` (топологія ухвалена + desktop-підрозділи застарілі), `02`/`CLAUDE`/`11.5`/`00`(v2.0)/`15`/`16`, `03`(T10). Виправлено функц. розходження: §8.4 detect_pass більше не пише price_daily (cagg), §8.8 DoD міграцій, §15 глосарій. | Розглянути **центрально-нативний перепис** §8.2/8.5/8.6 (процеси/бекап — зараз лише banner-deprecated) **АБО** `git init`+S0.1. |
| 2026-07-16 | Диригент | **Git-безпека проти витоку:** `07-conventions.md` (§Git — принцип «історія незворотна й може стати публічною», що не комітити, env-секрети, pre-commit скан, remote приватний, секрет→ротуй-перший, касети-очищені) + корінний `.gitignore` + рішення P12 + рядок у `CLAUDE.md`. | `git init` (тепер безпечний — `.gitignore` готовий) → S0.1 `probe.py` (Worker). |
| 2026-07-16 | Диригент | Створено корінний **`CLAUDE.md`** (лін, авто-завантажується): дистилят інваріантів A–D + правило «не використовувати агентів» + ↻-вказівник → captain-log. Робить відновлення автоматичним. Оновлено посилання в `05`/README. | `git init` (+`.gitignore`, root `README`) → S0.1 `probe.py` (Worker). |
| 2026-07-16 | Диригент | Створено `04-captain-log` з ↻-процедурою відновлення після скидування контексту (типи A/B, порядок читання, «перевір-не-вір-переказу», профілактика-чекпойнти). Перший запис стану. | Рішення оператора: `git init`+S0.1 (Worker) **чи** добити `06-backlog`/`07-conventions`. |
