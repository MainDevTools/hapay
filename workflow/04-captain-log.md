# Captain-log — стан проекту й відновлення після скидування контексту

**Статус: живий документ.** Це **пам'ять Диригента між сесіями** і **якір відновлення**, коли Claude Code скидає/компактує контекст. Один принцип: **репо — істина, пам'ять — одноразова.** Якщо цей файл і реальність (git, тести, `tasks/*`) розходяться — вір репо, не переказу.

**START HERE для свіжої/скинутої сесії:** спершу [↻ Процедура відновлення](#-процедура-відновлення), потім [Поточний стан](#поточний-стан).

---

## ↻ Процедура відновлення

Скидування буває **двох типів** — не плутати:

| Тип | Що це | Загроза |
|---|---|---|
| **A. Свіжа сесія** | ти навмисно почав нову (Диригент/Виконавець) | нема, якщо є що прочитати (цей файл + bootstrap §05) |
| **B. Авто-компакт** | харнес сам сумаризував довгу сесію **посеред задачі** | 🔴 переказ міг **загубити** інваріант, точний під-крок, незакомічене |

**B — справжня небезпека:** сесія прокидається з переказом і продовжує, **довіряючи йому**. Головне правило: **переказ — підказка, не істина.**

### Що прочитати (у цьому порядку)
1. **`CLAUDE.md`** (корінь, коли з'явиться) АБО `workflow/02-invariants.md` — **тверді інваріанти** (їх найлегше загубити в переказі, тож завжди перечитати);
2. **цей файл → «Поточний стан»** — де ми, що в роботі, що заблоковано;
3. якщо посеред задачі — **`tasks/SNN-*.md` + його §Outcome** — що вже зроблено/лишилось;
4. **`git log --oneline -15`** і **`git status`/`git diff`** — що реально закомічено vs у робочому дереві;
5. `workflow/03-decisions.md` — щоб **не перерішувати** вирішене (+ відкрите O1–O9);
6. за потреби — `16-reyestr-ryzykiv.md` §16.5 (актуальні ризики).

### Перевірити (не вір пам'яті)
7. **Прогнати тести / `probe.py`** → поточний стан коду з **факту**, а не з переказу;
8. звірити «що я нібито робив» (переказ) з п.3–4 (Outcome + git). Розбіжність → вір Outcome+git.

### Аж тоді продовжити
9. Якщо задача не закінчена — резюмуй у 1–2 рядки «де я і наступний крок», і **тільки потім** пиши код/текст.
10. Якщо не сходиться (тести червоні там, де Outcome каже «зелено»; git порожній там, де переказ каже «зробив») — **STOP**, признач це оператору, не імпровізуй.

> Свіжа сесія (тип A) з нуля: бери промпт зі `workflow/05-bootstrap-prompts.md` (Диригент або Виконавець) — він уже веде за цим списком.

---

## Профілактика (щоб було що відновлювати)

Скидування безболісне, лише якщо **нічого критичного немає лише-в-пам'яті**. Дисципліна:

- **Комітити кожен зелений крок**, не в кінці задачі → компакт/збій не з'їдає незакомічене. (потребує `git init` — P6, ще не зроблено).
- **Оновлювати §Outcome брифа інкрементально** («зроблено досі: …»), не лише наприкінці.
- **Свіжа сесія Виконавця на задачу** (O2) → задача коротка, **до авто-компакту не доходить**. Це головна профілактика типу B: не давати сесії розростись.
- **Shutdown-ритуал:** наприкінці сесії дописати рядок у [Журнал сесій](#журнал-сесій) (де спинились + наступний крок). Тоді наступна сесія стартує з факту, а не з археології.
- **Стан — у файли, не в чат:** рішення → `03-decisions`; розходження з реальністю → відповідний §спеки + журнал `00-index`; де ми → сюди.

---

## Поточний стан

> Оновлювати при кожній зміні стану. Це те, що читає скинута/свіжа сесія **першим по суті**.

**Дата оновлення:** 2026-07-16

**Фаза:** 🟢 **розробка почалась.** `git init` ✓ (P6 закрито), перший коміт `d2c46b3`. **Перший код: `probe.py` (S0.1)** — прожили перший Worker-цикл. `.gitignore` активний, чекпойнт-дисципліна ввімкнена.

**Спека:** `00-index.md` … `16-reyestr-ryzykiv.md`, версія **v2.0**. Реєстр ризиків — 208 пунктів із пріоритетами P0–P3.

**🔴 Велике рішення T10 (2026-07-16, O1 закрито):** двигун БД **SQLite → PostgreSQL 16 + TimescaleDB**, топологія **central-only** (desktop-local відкинуто). Ухвалено **власником проти рекомендації CC** (CC радив лишити SQLite+DAL до чисел S0.1; власник: 0 рядків = міграція безкоштовна). Канон переписано: `06` (hypertable/continuous-aggregate/компресія/`tsvector`/BIGINT/форвардні міграції), `08` (топологія ухвалена; §8.2/8.5/8.6/8.9 desktop — позначені **застарілими**, не видалені), `02`/`CLAUDE.md`/`11.5`/`00`/`15`/`16`, журнал `03-decisions` T10. **Провенанс дисенту збережено** (T10 + §00-changelog).

**Процес (`workflow/`):** створено `00-model`, `01-task-brief-template`, `02-invariants`, `03-decisions`, `04-captain-log` (цей), `05-bootstrap-prompts`, **`07-conventions`** (поки лише Git-безпека), `08-interaction-protocol`, `09-operator-guide`, `README` + корінний **`CLAUDE.md`** + **`.gitignore`**. Ще нема: **`06-backlog`**; `07`-решта (repo-map/Ready/«застряг»/рев'ю) і `CLAUDE.md`-guardrail-тести — з кодом.

**Що зроблено останнім:** captain-log + ↻-план → корінний `CLAUDE.md` → **Git-безпека проти витоку** (`07-conventions` §Git + `.gitignore` + P12): що не комітити, секрети через env, remote приватний, секрет-у-історії→ротуй-перший, касети лише очищені. Дистиляція в `CLAUDE.md`.

**Наступний реальний крок (за спаданням цінності):**
1. **`git init` (P6)** — без нього нема ні комітів-чекпойнтів, ні репо-як-шини; блокує всю профілактику вище. У тому ж кроці народити `.gitignore` + корінний `README.md`.
2. **S0.1 `probe.py` у Worker-режимі** (`tasks/S0.1-probe-liquidity.md`) — **перший рядок коду**; валідує ризик №1 (ліквідність) + #101 (персоналізоване ціноутворення) на живих даних. Пише в **локальний Postgres/Timescale** (не SQLite — T10). Досі жодного разу не робили — весь проект неваліда́ваний (0 коду, 0 ринкового сигналу).
3. Дописати процес: **`07-conventions`** (repo-map, Definition of Ready, «застряг», реальність-фідбек, git-конвенції, рев'ю) і **`06-backlog`** (S0…SN зі статусами/WIP=1).

**Заблоковано / чекає власника:** відкриті рішення **O1–O9** (`03-decisions`) — стратегія (топологія central-light, бізнес-модель, гібридний збір, open-source методології, межа «чи реальна» vs «де дешевше»). CC їх **не вирішує** — це 🧭 власника.

**Активна задача зараз:** — (S1 закрито). **Готово й верифіковано:** S1 `0001` схема + DB-пул + персист — **✅ 10/10 DoD проти живого Timescale в CI** (run 29495439389). S0.1/S2 — `in-review`. **Наступне: S3 `detect_pass`** — обчислення бейджів (§5/§8.4) поверх `price_snapshot`: Стадія A (заявлена) + Стадія B (30-денний MIN із сирого) → upsert `discount_event`. Схема й персист уже є.

> **Середовищний блокер — ЗНЯТО через CI.** Локального Timescale немає (нема Docker/WSL; Timescale без Windows-білда), але **CI job `migration`** (service `timescale/timescaledb:latest-pg16`) верифікує БД-код живцем на кожен пуш. Репо: `github.com/cavemancinema/radar-znyzhok` (приватний, `main`). `gh` автентифіковано (cavemancinema) → я читаю CI-логи й ітерую сам. **Патерн для всього БД-коду далі:** писати → пуш → CI верифікує → правити. probe-cron — 06:17 UTC / вручну.
>
> **Node20-депрекація** (annotation CI): `actions/checkout@v4`/`setup-python@v5` форсяться на Node24 — косметика, не блокер; оновлять мейнтейнери.

---

## Журнал сесій

> Append-only. Shutdown-ритуал дописує рядок: дата · режим · що зроблено · де спинились · наступний крок. Найновіше зверху.

| Дата | Режим | Що зроблено | Наступний крок |
|---|---|---|---|
| 2026-07-16 | **Виконавець** | **S1 ВЕРИФІКОВАНО ✅** — запуш на GitHub (приватний), `gh` авторизовано, CI job `migration` прогнав `0001` проти живого Timescale: **10/10 DoD** (hypertable/cagg/tsvector/append-only/round-trip), 0 правок. Блокер знято через CI. | **S3 `detect_pass`** (бейджі §5) поверх наявних схеми+персисту. |
| 2026-07-16 | **Виконавець** | **S1 `0001` схема + DB-пул (code-complete).** `migrations/0001_init.sql` (уся §6: hypertable/cagg/компресія/tsvector/append-only тригер/BIGINT), `db/pool.py`+`migrate.py`+`store.py`, skip-aware `test_migration` (DoD §6.6 + round-trip RawItem). requirements+psycopg. **БЛОКЕР:** нема локального Timescale (нема Docker/WSL) → верифікація в CI (додав job `migration` з timescale-сервісом). Локально 18/18, migration skip. | **Пуш на GitHub** → CI прожене `0001` живцем (або зовн. Timescale). Потім правки за першим живим прогоном, далі S3 `detect_pass`. |
| 2026-07-16 | **Виконавець** | **S2 адаптер Pethouse (екстрактор на касеті).** `adapters/base.py`+`pethouse.py` (selectolax), обрізана реальна касета, `test_pethouse` 9/9, `requirements.txt`, CI-гейт `tests.yml` (18/18). Reality-фідбек: картки **мультиваріантні** → RawItem/варіант, `external_ref=url#v=<варіант>` (§4.8/GAP15); вписано §3.3. S2→`in-review`. | Рев'ю S2; далі **S1 `0001` схема + DB-пул** для персисту RawItem, тоді `detect_pass`. |
| 2026-07-16 | **Виконавець** | **Старт розробки (S0.1).** `git init`+перший коміт `d2c46b3`. `probe.py` (stdlib, сигнал-орієнтований) + тести 9/9 + CI-cron. Живий прогін 6 крамниць: **Pethouse+PetChoice працюють** (тир A, old=168/74), решта — тир C/лендинг. Реальність-фідбек у §3.3+Outcome: Pethouse редизайн, калібровано хибні anti-bot, #101 ціни cookie-стабільні. S0.1 → `in-review`. | Рев'ю S0.1 (Диригент); далі S2 адаптер Pethouse — але під **нові** селектори (line-through/price-field), не застарілі §3.3. |
| 2026-07-16 | Диригент | **§8.2/8.5/8.6 переписано central-native** (не banner-deprecated): §8.2 — GH Actions cron + concurrency-group, майстер-розклад у БД, «тиха смерть» через failed-run сигнал, довіра до годинника; §8.5 — deploy-bootstrap (провізія БД→міграції→сід) + Telegram-онбординг без акаунтів; §8.6 — PITR+`pg_dump`+перевірка-відновленням, Timescale-компресія, реконсиляція орфан-scan_run. Топологічно-нейтральні інваріанти (worker-queue/per-host/буфер→txn) збережено. §00-changelog оновлено. | `git init`+S0.1 (Worker) — канон готовий; **АБО** дрібний хвіст: §7.7/§10 desktop-privacy у напрузі з central-only (радше O5). |
| 2026-07-16 | Диригент | **T10 — двигун БД SQLite→PostgreSQL 16 + TimescaleDB, central-only (O1 закрито).** Рішення власника проти рекомендації CC. Переписано канон: `06` повністю (hypertable/cagg/компресія/tsvector/BIGINT/форвардні міграції), `08` (топологія ухвалена + desktop-підрозділи застарілі), `02`/`CLAUDE`/`11.5`/`00`(v2.0)/`15`/`16`, `03`(T10). Виправлено функц. розходження: §8.4 detect_pass більше не пише price_daily (cagg), §8.8 DoD міграцій, §15 глосарій. | Розглянути **центрально-нативний перепис** §8.2/8.5/8.6 (процеси/бекап — зараз лише banner-deprecated) **АБО** `git init`+S0.1. |
| 2026-07-16 | Диригент | **Git-безпека проти витоку:** `07-conventions.md` (§Git — принцип «історія незворотна й може стати публічною», що не комітити, env-секрети, pre-commit скан, remote приватний, секрет→ротуй-перший, касети-очищені) + корінний `.gitignore` + рішення P12 + рядок у `CLAUDE.md`. | `git init` (тепер безпечний — `.gitignore` готовий) → S0.1 `probe.py` (Worker). |
| 2026-07-16 | Диригент | Створено корінний **`CLAUDE.md`** (лін, авто-завантажується): дистилят інваріантів A–D + правило «не використовувати агентів» + ↻-вказівник → captain-log. Робить відновлення автоматичним. Оновлено посилання в `05`/README. | `git init` (+`.gitignore`, root `README`) → S0.1 `probe.py` (Worker). |
| 2026-07-16 | Диригент | Створено `04-captain-log` з ↻-процедурою відновлення після скидування контексту (типи A/B, порядок читання, «перевір-не-вір-переказу», профілактика-чекпойнти). Перший запис стану. | Рішення оператора: `git init`+S0.1 (Worker) **чи** добити `06-backlog`/`07-conventions`. |
